{
  "name": "marketplace_subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ Marketplace Subgraph",
    "create-local": "graph create --node http://localhost:8020/ Marketplace Subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ Marketplace Subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Marketplace Subgraph"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.2.1",
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "@openzeppelin/contracts": "^4.2.0",
    "solc": "^0.7.3"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-etherscan": "^2.1.4",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.4",
    "hardhat": "^2.6.0"
  }
}
